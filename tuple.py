# Changing the tuples
index_tuple = (4, 2, 3, [6, 5, 7])  # The index of tuple carries numbers, where [6,5,7] are mutable indexes which can be changed.
index_tuple[3] [2] = 10    # Here in index 3 , the value at 3rd index will be changed,
print(index_tuple)
